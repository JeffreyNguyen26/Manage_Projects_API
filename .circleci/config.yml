version: 2.1
jobs:
  deploy:
    orbs:
      win: circleci/windows@2.2.0
    resource_class: windows.medium
    steps:
      - checkout
      - run:
          shell: powershell.exe -ExecutionPolicy Bypass
          name: "Deploy"
          command: |
              Get-InstalledModule
workflows:
  version: 2.1
  workflow:
    jobs:
      - deploy
